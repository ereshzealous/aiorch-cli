{
  "total_tests": 8,
  "total_failures": 8,
  "groups": [
    {
      "root_cause": "Authentication service responding with 500 instead of expected errors",
      "affected": [
        "tests/test_auth.py::test_register_duplicate_email",
        "tests/test_auth.py::test_login_wrong_password",
        "tests/test_auth.py::test_refresh_token_rotation"
      ],
      "next_action": "Check the Uvicorn/FastAPI logs for the authentication service to identify the specific 500 error details."
    },
    {
      "root_cause": "PostgreSQL service is unreachable or overloaded",
      "affected": [
        "tests/test_connectors.py::test_postgres_connection_timeout"
      ],
      "next_action": "Verify if the PostgreSQL container/service is running and accessible on localhost:5433, checking its logs for any startup errors or resource exhaustion."
    },
    {
      "root_cause": "Several tests failed with unknown errors, possibly due to environment issues or unhandled exceptions",
      "affected": [
        "tests/test_runs.py::test_run_concurrent_admission",
        "tests/test_pipelines.py::test_yaml_parse_malformed",
        "tests/test_scheduler.py::test_spend_reconcile_updates_redis",
        "tests/test_webhooks.py::test_webhook_rate_limit"
      ],
      "next_action": "Examine the full CI logs for each of these tests to find stack traces or more specific error messages that were not captured by the summary."
    }
  ]
}